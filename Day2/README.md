Block A — Iteration
• for loops over sequences, range() with all three arguments
• while loops, avoiding infinite loops
• break, continue, pass, and the rarely-known else clause on loops
• Nested loops and when to avoid them
• enumerate() and zip()

Block B — Functions
• Defining and calling; positional, keyword and default arguments
• The mutable-default-argument trap (def f(x=[])) — why it bites
• *args and **kwargs, argument unpacking
• Return values, returning multiple values via tuples
• Scope: local, enclosing, global, built-in (LEGB); global and nonlocal
• lambda expressions and where they're appropriate
• Recursion basics and its limits
• Docstrings, PEP 8, meaningful naming
