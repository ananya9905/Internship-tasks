Setup, Syntax and Control Flow

Block A — Environment and basics
• Installing Python 3.12+, verifying with python --version
• The REPL vs running .py files; VS Code setup and the Python extension
• Variables, assignment, dynamic typing, id() and object references
• Core types: int, float, str, bool, None, plus complex
• Arithmetic operators, integer vs float division, %, **, operator precedence
• Type conversion: int(), float(), str(), bool() — and where it fails
• Comments, indentation rules, why whitespace is syntax in Python

Block B — Strings and decisions
• String indexing, slicing, immutability, escape sequences
• Key methods: upper, lower, strip, split, join, replace, find, startswith
• f-strings, .format(), format specs (:.2f, :>10, :,)
• input() and print() with sep, end
• if / elif / else, nested conditions
• Comparison and logical operators (and, or, not), chained comparisons
• Truthiness: which values are falsy and why it matters
• Ternary expressions

Deliverable: a CLI unit converter (temperature, distance, weight) with input validation.
Self-check: Why does 0.1 + 0.2 != 0.3? What's the difference between is and ==?
